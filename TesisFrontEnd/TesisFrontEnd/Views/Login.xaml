<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:TesisFrontEnd.ViewModels"
             mc:Ignorable="d"
             x:Class="TesisFrontEnd.Views.Login">

        <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Login" Text="{Binding Username, Mode=TwoWay}"/>
            <Entry Placeholder="Password" IsPassword="True" Text="{Binding Password, Mode=TwoWay}"/>
            <Button Text="Log In" Command="{Binding LoginCommand}"/>
            <Button Text="Register" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>